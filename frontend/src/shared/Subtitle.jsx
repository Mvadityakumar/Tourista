import React from "react";

const Subtitle = ({ subtitle }) => {
  return (
  <h3 style={{color:"white",  }}  className="section__subtitle">
    {subtitle }
  </h3>
)};

export default Subtitle;
